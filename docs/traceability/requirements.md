# LifeOps Web Requirement Traceability

**Authority:** ADR-016 through ADR-030, final redesign spec, life-domain design, execution-completeness design, image-delivery-boundary design, master plan and ordered P1–P6 plans.
**Status vocabulary:** `pending | in_progress | api-complete/ui-pending | verified-local | verified-image | verified-registry | blocked`.  
**Latest task-boundary truth:** P6-T7 is formally closed and P6-T8 Step 8 is the sole current boundary. Steps 1–6 completed the 44-parent reverse audit, fake/placeholder classification, one clean-install final gate, product/operations documentation, the 44-row final verification index, repository-backed project-close manifest and deterministic evidence refresh. Step 7 completed the primary-agent code/design review, accepted and fixed the missing exact-digest browser gate plus its secure-context boundary, and reran all affected focused gates. The validator-derived parent truth is 44/44: 34 `verified-local` and 10 `verified-image`; registry-final DELIVERY-01 atoms are complete while the mixed-boundary parent correctly remains `verified-local` by the least-child rule. Fresh gates pass frontend 88 files/429 tests; server 362 ordinary plus 50 official MySQL 8.4.10 exact-integration tests; both typechecks/builds; source-current official Linux Playwright WebKit theme 1/1, Firefox theme 1/1 and matrix 336/336 with one worker/zero retry; real-Fastify production-preview Chromium/Firefox/WebKit 12/12; and a separate exact-released-digest gate with WebKit theme 1/1, Firefox theme 1/1, applicable public/visual/accessibility matrix 133/133 and real MySQL/API Chromium/Firefox/WebKit 12/12, again with one worker/zero retry. The exact browser gate uses a browser-container `127.0.0.1` trusted origin that forwards only to the same-run exact Web/API proxy and explicitly proves `isSecureContext` plus UUID-backed persisted task writes; its evidence SHA-256 is `4697559DD336F77782B537084AD4ED34CF3646460D460FD5C111422CD54B054C`. Lighthouse 1.00/1.00/0.96/0.91, Helm 4.2.4 strict lint, exact UHub digest startup/API smoke, live registry/release-manifest inspection and deployment-package negative/positive contracts also pass. Ordinary CI `33305202070` passed those application, MySQL, browser, Helm and contract gates, then exposed a production-image-only builder context gap: `tsconfig.node.json` referenced three Playwright image/remote configs that the root Dockerfile had not copied. A focused behavioral RED locked the missing boundary; the minimal Dockerfile copy fix then passed 17/17 focused tests, the complete 429-test frontend suite and isolated Web/API image builds without changing product behavior or release digests. The first Linux matrix was 335/336 only because Windows `git archive` staged two exact CSS entry files as CRLF; normalizing the disposable Linux volume to the repository LF contract made the named test and complete matrix green without changing product, worker, retry, threshold or coverage. Eight fresh 1440×900/390×844 day/night rest/login images were opened individually and retain complete rings, approved login depth, dark night login, mobile safe inset, zero overflow and the unlit `05 / 此刻正在发生` center. Root deployment entry, release facts, backup/restore, media, observability, rollback, product and design documentation are integrated; local link scan, unsafe-command scan and the real package validator pass. The immutable release remains ordinary CI `33285063683`, release `33286877080`, source `64cb76932def9eed94cb43aea104c97eb19f1382`, GitOps `03d812339cb42bfb3633ad613b4dd55509fd0084`, Web `sha256:31d13ed140d0f3343bbef40355e736ce8d63298ffa3c3efb97f27659fb9fa4af` and API `sha256:c70d0b33612e36c171c4085639e8cf7d558abdbd37b780fb0bd651a4e7c9c5e3`. The remaining verification-before-completion cannot change these release facts without reopening the boundary; no cluster, Argo, DNS/TLS or production-reachability result is claimed.
**P6-T7 approved contract formalization:** The P6 work package, source registry and acceptance matrix carry 17 reciprocal unfamiliar-cluster atoms for architecture/terminology, capability-first preflight, the user-owned cluster boundary, Secret/ExternalSecret handoff, four database branches, media branches, controller/Ingress branches, immutable inputs, offline preflight, Argo/Helm ordering, user smoke, operations/restore, rollback, scaling, command safety, repository asset mapping and the approved fourteen-section manual order. The matrix is 44 parents / 52 surfaces / 1,444 atoms. Current P6-T7 unit, delivery-package, security, manual and registry evidence verifies every applicable new atom except the two deliberate P6-T8 integration boundaries above; the validator-derived parent rollup is therefore 30/11/3 without mechanically promoting old evidence.
**Current CI18 prepaint override:** The bounded implementation/current-evidence commit is `80cb78998a24329168dbcd3162317b3d9c29c9eb`; `origin/main` and verified remote `main` remain `56611eafed0c6e46c825c309fa3b3eb3f399c892` while the narrow ledger/checkpoint atom is prepared. Ordinary CI run `33261353542` / job `99123743985` passed frontend unit/type/build, official MySQL and browser installation, then failed only WebKit theme performance because the decoded raster remained hidden until first theme use; baseline P95/max was 18/18 ms and transition P95/max was 53/81 ms against unchanged 35/100 ms budgets. The bounded correction keeps the decoded 1440×900 raster visible beneath the day overlay and enables theme switching only after two paint frames; worker, retry, browser, threshold, sample duration, geometry, ring period and motion rate remain unchanged. Fresh current-source gates pass affected theme contracts 19/19, frontend 88/88 files and 425/425 tests, typecheck and an 885-module build; official Linux Playwright 336/336; real-Fastify 12/12; Lighthouse launcher 1/1 and official Linux Lighthouse 1.00/1.00/0.96/0.91; source-current server 362 ordinary plus 50 exact-only skips, official MySQL 50/50, 16-migration dump/restore, Helm/media/security/observability/workflow/release contracts, audits and current-source image smoke. All eight CI18 images were opened individually and a separate eight-state numerical audit passed. P6-T6 Step 7 and 30/10/4 remain unchanged; no UHub digest, SBOM/provenance, exact-digest smoke, release success, DNS/TLS or cluster state is claimed.
**Current CI16 worker-remediation override:** The bounded CI16 implementation and ledger commits `f5217fcbaa08c2af75ad43b514253bd1909ec2b4` and `e8c397d1ed1fe19f72134cd3d6748ae514921b07` are pushed; local `HEAD`, `origin/main` and verified remote `main` were aligned at `e8c397d1ed1fe19f72134cd3d6748ae514921b07`. Ordinary CI run `33260290566` / job `99120946936` then failed only in the frontend unit portion: two lazy-route assertions reached their unchanged five-second query boundary under `maxWorkers: 4`, while their private shell remained stable; MySQL, browser and later gates did not run, and no release was dispatched. The two affected files pass 21/21 alone. A focused delivery-harness contract failed exactly on the four-worker setting and passes after the ceiling changed to `2`, while retaining `testTimeout: 20_000`, forbidding `hookTimeout`, and changing no assertion, coverage rule or test count. Raw `npm.cmd test` now passes 88/88 files and 425/425 tests; frontend typecheck and the 885-module production build pass. Product, server, browser, Helm, image and visual sources are unchanged from the complete current-source CI16 replay, so their prior local evidence remains source-current rather than being represented as part of the failed ordinary run. A new ordinary CI must be genuinely green before the one authorized `1.0.0` dispatch. P6-T6 Step 7 and the 30/10/4 parent truth remain unchanged; no UHub digest, SBOM/provenance, exact-digest smoke, release success, DNS/TLS or cluster state is claimed.
**Post-implementation Git override:** The bounded CI16 implementation/current-evidence commit is `f5217fcbaa08c2af75ad43b514253bd1909ec2b4`; `origin/main` remains `63483511cbed0ddb198a851b595d87656cbcdfd1`, and the repository commit containing the post-implementation checkpoint is the final narrow local ledger atom before the authorized push. This newer Git boundary supersedes only the pre-correction Git identity in the following task-boundary paragraph; all P6-T6 Step 7, 30/10/4 and release-gating facts remain unchanged.
**Latest task-boundary truth:** P6-T5 remains formally closed and P6-T6 Step 7 is the sole current boundary at 30/44 (30 `verified-local`, 10 `partial`, 4 `pending`; no parent promotion). Local `HEAD` and `origin/main` are `63483511cbed0ddb198a851b595d87656cbcdfd1` before the current CI16 correction. Ordinary CI run `33255141653` / job `99107422687` passed unit/type/build, official MySQL 8.4 and browser installation, then failed only the dedicated WebKit theme-performance test: baseline P95/max 17/19 ms and transition P95/max 52/70 ms against the unchanged 34/100 ms budgets. CSS already consumed `public-stars-raster.png`, but the hidden preload still decoded `public-stars.svg`; focused TDD passed 13 assertions and failed exactly two until that hidden image decoded the exact raster consumed by CSS. A four-worker Vitest ceiling also makes unchanged lazy-route assertion budgets deterministic on the local 16-core memory-constrained host; it changes no timeout, assertion or coverage rule. Fresh current-source gates pass combined focused 21/21; frontend 88/88 files and 425/425 tests, typecheck and an 885-module production build; server 362 ordinary tests plus 50 exact-only skips, typecheck/build; official MySQL 8.4.10 50/50; Helm/media/security/observability/workflow/release contracts; current-source Web/API image smoke and data rehearsal; official Linux Playwright WebKit theme 1/1 repeated 10/10, Firefox theme 1/1 and one corrected complete matrix 336/336 for 338/338 in 25.8 minutes with workers=1/retries=0; real-Fastify 12/12; Lighthouse 1.00/1.00/0.96/0.91. The first complete matrix attempt passed all 334 behavior cases and failed only two artifact writes because `/work/outputs` was deliberately read-only; an owned writable outputs volume then passed the entire visual-capture file 6/6 and the complete matrix 336/336. The primary executor opened all eight CI16 1440×900 and 390×844 day/night rest/login frames individually at original resolution and confirmed exact raster preloading, visible stars, complete rings, approved orbit-left/title-recede depth, dark night login, mobile safe inset, zero overflow and the unlit `05 / 此刻正在发生` center. The eight protected older untracked files remain untouched. A deterministic checkpoint is generated only after this boundary and task paths are synchronized; all 462 evidence IDs retain exact order. Registry-bound P6-T6 atoms remain open, so the rollup stays 30/10/4. The user's continuing authorization covers commit/push and exactly one `1.0.0` dispatch only after a new ordinary CI is genuinely green. No UHub image, digest, SBOM, provenance, release success, DNS/TLS or cluster state is claimed before those external results exist.
**P6-T1 delivery evidence:** `createRuntime` no longer mutates schema; the dedicated `migrate` entry runs ordered checksum-locked migrations once, closes its pool on success/failure, sanitizes errors and returns non-zero on drift/database failure. Filesystem and S3 backends share the exact 10 MiB/MIME/signature/checksum/random-key contract; S3 objects use conditional private puts, bounded head/range reads, missing-object mapping and strict credential-free errors with no fallback. Helm 4.2.4 renders an Argo `PreSync` Job with wave `-10`, finite deadline/backoff, API image parity and no ServiceAccount token; filesystem RWX and S3 Secret-ref topologies pass while multi-replica RWO fails. Focused server passes 25/25, full server passes 356 ordinary tests plus 50 separately proven exact-only tests, official isolated MySQL 8.4.10 passes 50/50 zero skip, dual server typecheck/build pass, Helm lint/topology pass, and Docker/Buildx builds and inspects the non-root local API image at `sha256:2de0391556b5a2f3d47590087c5335444d1e39d5fcafb38e46be923a05f2dec3`. That digest is local-only and is not UHub, registry, SBOM, provenance or formal release evidence.  
**P6-T2 security evidence:** Helm 4.2.4 renders no platform ServiceAccount/token/RBAC while Kubernetes integration is disabled and, when enabled, renders one 600-second projected token/CA mount plus the exact get/list-only nodes, pods/services/namespaces, apps workloads and Gateway API surface. Production schema rejects inline Secret data and simultaneous Ingress/HTTPRoute; credentials use `secretKeyRef`, non-secret configuration uses a checksum-bound ConfigMap, and all workloads use numeric non-root identity, RuntimeDefault seccomp, read-only roots, no privilege escalation, drop-ALL capabilities, finite resources and bounded termination. LifeOps-labelled default isolation avoids unrelated namespace Pods; Gateway, Prometheus, DNS, internal/external MySQL and configured integration CIDR/ports are explicit, while `0.0.0.0/0` requires an explicit reviewed override. Focused rendered security, Helm lint, prior media topology and default/production validators all pass. Production validation reports `render-contract-fixture-release-pending`: its deterministic in-memory digest-shaped values are never written to production values and are not UHub/image/SBOM/provenance evidence.  
**P3-T5 approved interface contract:** Markdown rejects raw script/iframe/event attributes and unsafe protocols while preserving GFM table/task semantics and exact external-link `rel`; autosave exposes all six states with an exact 800 ms debounce, visible/timestamped state, honest session-local plain-draft privacy, leave flush, 409 recovery and duplicate suppression. ADR-026 persisted nullable `coverMediaId`: create defaults to null; PATCH omission preserves, null clears and non-null selects only a same-owner image included in the record `mediaIds`; removing the active cover clears or replaces it in the same versioned transaction, while private-media authorization remains unchanged. The page decodes one `<goal|project|task|habit>:<non-empty-id>` source once, splits at the first colon, and maps only to existing `linkType + linkId`; malformed or duplicate values remain a scoped validation error and send no malformed request. `012_record_cover_identity.sql` is additive, applied 001–011 are immutable, and uncreated knowledge/publishing/search/settings migrations are 013–016. All behaviors are now covered by current unit/API/MySQL/E2E/security/accessibility and opened responsive evidence.  
**Current product evidence:** The final CI16 local replay passes Web 425/425, server 362 ordinary plus exact MySQL 50/50, Web/server typecheck and production builds, official Linux Playwright 338/338 with workers=1/retries=0, and real-Fastify remote browser 12/12 in the official Linux container. Helm/media/security/observability/workflow/release-contract checks, root/server production audits, current-source Web/API image smoke and MySQL dump/restore rehearsal pass. The final Web image retains all three non-empty repository-owned public SVG sources and uses the exact transparent 1440×900 night-star raster derived from `public-stars.svg`; both CSS and the hidden decode gate now reference that same raster. Its 8,814-byte SHA-256 remains `83B997E0E1C13D62BF3E00787A2A9AC4F968F6AAC5D3BE279378E3BC4729A309`. Local tags and image IDs remain local-only and are not immutable registry digests. Lighthouse is 1.00 performance, 1.00 accessibility, 0.96 best practices and 0.91 SEO in the official Linux browser image with its verified Chromium path, without changing rendering or budgets. Eight current 1440×900/390×844 day/night rest/login images were opened individually at original resolution and preserve visible stars, the approved four naturally rotating rings, desktop orbit-left/title-recede login depth, mobile full-screen task layering, complete outer-ring safe inset, dark night login, zero horizontal overflow and the plain unlit `05 / 此刻正在发生` center. These are current local source/image proofs only; no UHub, exact digest, SBOM, provenance, release, DNS/TLS or cluster claim is made here.
**P3-T8 product evidence:** stable `/app/life` and `/app/life/calendar` routes now live inside the unchanged private shell; the exact eight-item Life subnav maps inventory to `/app/life/ingredients`. Today keeps the timeline primary, separates planned/actual nutrition and consumption cost from cash expenditure, elevates only actionable shortage/conversion notices and turns no data into explicit next steps. Calendar provides semantic month states, sequential mobile summary, local retry, plan-only copy, roving keys, focus trap/Escape/Back restoration and a persistent close control whose background/visibility geometry is browser-asserted after deep scroll. Fresh focused unit is 11/11, full Web is 57 files/252 tests, Life Chromium is 3/3, the rebased original-private gate is 14/14, remote Fastify browser is 3/3 and all newly generated P3-T7/P3-T8 named artifacts were opened. These rows add current local child evidence but do not bypass P3-T13, exact-image or later Life-route requirements; parent completion remains 4/44.  
**P3-T10 product evidence:** `/app/life/recipes` now keeps recipe index, route-addressed inspector, keyboard-reorderable editor, serving scaling, pinned history and version-impact preview on one continuous Daylight canvas. Incomplete conversion/nutrition/price facts stay explicit and block cooking. Cooking persists timers and actual quantities, resumes the same session, keeps notes session-local until explicit promotion, and previews inventory consumption, eaten portions, prepared-food remainder, nutrition and cost before one completion. Bidirectional ingredient↔recipe navigation retains a list fallback beside interactive graph nodes and exposes factual now/missing-one/expiring filters. Fresh focused Web is 11/11, focused recipe server is 22/22, full Web is 59 files/274 tests, full server is 217 pass plus 45 separately proven exact-only tests, official MySQL 8.4.10 is 45/45 zero skip, Life Chromium is 11/11, ordinary P2/P3 Chromium is 57/57, and all 16 recipe screenshots/filmstrips were opened.  
**P3-T11 product evidence:** `/app/life/plans` now presents one continuous desktop week canvas and mobile day-first view with custom slots, drag/menu parity, portion/time edits, supplement meal-time linkage, explicit template keep/merge/replace/skip preview, bounded sync range and plan-only date copy. Medicine actions remain user-fact-only; `/app/life/fitness` composes user-maintained activities, labels estimated energy separately, records actual duration once and reverses completion. The additive `PATCH /api/v1/life/day-plans/:date` contract preserves owner scope, plan/item versions, stable server IDs and immutable completed history in Memory and existing MySQL JSON. Fresh focused UI is 7/7, focused planning domain/route is 34/34, typed client is 6/6, full Web is 61 files/281 tests, full server is 220 pass plus 45 exact-only skips, official isolated MySQL 8.4.10 is 45/45 zero skip, Life Chromium is 15/15 and ordinary Chromium is 57/57. The primary agent opened all planning and fitness captures at 1440/1024/768/390/320, 200% zoom and reduced motion plus five interaction filmstrips.  
**P3-T12 product evidence:** `/app/life/shopping` distinguishes suggestions, formal rows and history, merges reasons, records partial purchase with actual quantity/amount/batch/expiry/location and supports a compensating refund. `/app/life/analytics` keeps cash and consumption cost separate, distinguishes no record from zero, presents budget thresholds and supplies an accessible table/source drill-down beside the SVG. `/app/life/data` exports a manifest with private-attachment boundaries, requires preview before write, creates a replace restore point and reports exact failed rows without mutating live data. Fresh focused UI is 7/7, focused commerce/portability server is 33/33, full Web is 64 files/288 tests, full server is 220 pass plus 45 exact-only skips, official isolated MySQL 8.4.10 is 45/45 zero skip, Life Chromium is 19/19, ordinary Chromium is 57/57 and real Fastify is 3/3. The primary agent opened every 1440/1024/768/390/320, 200% zoom and reduced-motion capture plus three interaction filmstrips.  
**P3-T13 product evidence:** the five comprehensive specs reuse the canonical P3-T8 through P3-T12 stateful fixtures and add a pending/duplicate/offline calendar-copy assertion. The valid RED showed the copy dialog closing before server confirmation; the minimal fix keeps the dialog, target date and one disabled submission until success, then exposes a scoped offline alert. A second ordinary-regression RED exposed leaked remote API mode; the final conditional setup passes the affected original group 7/7 and keeps explicit P3-T13 runs remote without changing the ordinary 57-test fixture suite. Fresh gates are calendar 6/6, comprehensive Life 20/20, canonical Life 19/19, full Web 64 files/288, full server 220 pass plus 45 exact-only skips, official isolated MySQL 8.4.10/34034 45/45 zero skip, ordinary Chromium 57/57 and real Fastify 3/3. The primary agent regenerated and opened all eight contact sheets covering 89 named screenshots/filmstrips across the Life routes.  
**P3-T14 closure evidence:** the reverse audit traces each original private and LIFE parent to domain/API or persistence, page/state tests, E2E and opened visual evidence. Six broad unit/API/MySQL/E2E/accessibility/security groups plus ten reviewed visual/manual groups aggregate only already-passing P3-T8 through P3-T13 evidence. The resulting 306-row manifest derives 488 verified-local + 434 partial + 498 pending atoms and 22 verified-local + 10 partial + 12 pending parents. The audit closes the formerly under-selected LIFE-03/04/05/06/11/17/18 evidence links but deliberately leaves image-bound APP-01/RECORD-01/LIFE-01/LIFE-19/LIFE-23/LIFE-24 partial, P5-bound LIFE-21 partial and P4-bound LIFE-20 pending. `outputs/final/private-core-verification.md` records exact gates, reproducibility metadata, every failure-state group and the 30-parent reverse map.  
**P4-T2 product evidence:** `/app/knowledge?collection=&tag=&source=&q=&note=<id>` now provides the approved continuous 2.5/3.5/6 library/list/reader-editor workbench and mobile list→reader layers. Safe Markdown, factual source/related links, 800 ms autosave, editor-scoped 409/offline recovery, review dates, resurfacing, pin/favorite/archive/delete/same-ID restore, keyboard/Back/focus/scroll restoration, 200%/320 CSS px reflow and reduced motion pass 11/11 focused unit, 4/4 route API, 302/302 Web, 228 server, 3/3 focused Chromium, 60/60 shared Chromium and 4/4 real Fastify browser gates. All four standard viewport captures plus the reduced-motion and offline states were opened after final generation.  
**P4-T3 product evidence:** exact `fflate@0.8.3` supports deterministic approved-path ZIPs; frontmatter and imports reject unsafe IDs, type/folder mismatches, invalid facts and zip-slip paths; the pure planner classifies both-side revisions/conflicts without propagating absence as deletion. Folder handles are stored only after granted read-write permission, the initial scan makes no writes, affected files are backed up before the first temp-write/replace, and a failed batch stops with exact completed/failed paths while retaining backup. Final focused tests pass 24/24, full Web passes 72 files/326 tests, typecheck/build pass, the existing shared Chromium regression passes 60/60 and the standalone settings Chromium harness passes fallback, permission denial, first-scan preview, explicit apply, keyboard, 1440/1024/768/390, 320 CSS px at 200% text and reduced motion. The primary agent opened every final P4-T3 capture; real route integration remains explicitly owned by P4-T6 rather than being claimed early.  
**P4-T4 product evidence:** domain/Fastify/scheduler RED was 13/13 and the typed-client RED was 3/3; the scheduler lifecycle extension separately failed only its missing fixed secret-free log. Final focused gates are server 15/15 and client 3/3. Full server passes 239 with 48 exact-only skips, exclusive full Web passes 73 files/329 tests, both typechecks/builds pass and the unchanged real Fastify Chromium boundary is 4/4. Official task-only MySQL 8.4.10/34036 passes 48/48 with migration 014, reconnect/owner/version/history and two-replica one-revision evidence; shutdown exits 0, subsequent ping exits 1 and PID/listener are absent. These rows make 21 backend publishing/security atoms partial but deliberately do not claim P4-T5/P4-T6 UI/E2E/image work.  
**P4-T5 product evidence:** the loadable workbench/public-page RED retained 15 existing passes and failed 11 missing behaviors. The final source/editor/preview implementation passes 26/26 focused tests, route API 6/6, full Web 74 files/341 tests, full server 239 pass plus 48 exact-only skips, both typechecks/builds and a 22-check real Fastify/Chromium harness. The browser proves save/reload, private-source absence, current-version confirmation reset, 409/offline local preservation, source→edit→preview focus return, featured ordering, stable `/p/:slug`, theme, Back, reduced-motion 0s, 1440/1024/768/390, 320 CSS px and 200% zoom without overflow. The primary agent opened all six final full-page captures; an initial 1024/768 compression and mobile lower-layer disclosure were corrected and every final capture passed hierarchy, density, alignment, overflow and interaction-clarity review.  
**P4-T6 product evidence:** the formal knowledge/Obsidian/publishing RED exposed four genuine gaps after selector-only corrections: `/app/settings` was a placeholder, conflicts had no explicit per-item choice gate, draft edits did not update public preview live and query invalidation could duplicate a new draft. The minimal implementation adds the real settings route, permission-gated preview/apply integration, explicit keep-Web/keep-Obsidian/keep-both decisions, live form-derived preview and stable draft deduplication. Fresh gates pass Web 74 files/341 tests, server 239 pass plus 48 exact-only skips, official task-only MySQL 8.4.10/34037 at 48/48 zero skip with 14 migrations, dual typecheck/build, focused cross-domain Chromium 13/13, real Fastify Chromium 4/4 and complete ordinary Chromium 69/69. The isolated database shut down normally and post-run ping/PID/listener checks prove it is absent. All 36 knowledge/settings/publishing/public PNGs were opened; rejected reduced-filmstrip cropping, 200% public-detail columns and fixed-control overlap were corrected, and the final set passes hierarchy, composition, typography, color and craft review.  
**P4-T7 product evidence:** Life recipes, cooking notes, fitness summaries, reviews and selected shopping/budget summaries now project to deterministic strict Markdown with stable allowlisted paths/frontmatter while raw inventory transactions, idempotency values, sessions, platform payloads and credentials remain excluded. FSA connection refreshes current P1 export facts before a read-only first scan; all differences remain preview-only until explicit choices, recipe edits require create-version intent, vault writes back up first and neither FSA nor ZIP absence propagates as deletion or mutates MySQL facts. Fresh gates pass focused Web 43/43, full Web 77 files/352 tests, server 239 pass plus 48 exact-only skips, official MySQL 8.4.10/34039 at 48/48 zero skip with 14 migrations, dual typecheck/build, focused cross-domain Chromium 19/19, affected P3 Life Chromium 20/20, ordinary Chromium 75/75 and real Fastify 4/4. The primary agent opened all eight final PNGs after correcting capture origin, mobile tabs, active-route visibility and Back-focus behavior; the final set passes hierarchy, composition, typography, color and craft. LIFE-20 advances only to `partial` because its four atoms still require the later immutable-image evidence owned outside P4.  
**P4-T8 closure evidence:** `outputs/final/content-verification.md` records the browser/OS/font/DPR/viewport/color/reduced-motion environment, dependency-lock hash, all P4 screenshot/filmstrip/trace paths, deterministic ZIP checksum, conflict/backup order, immutable publication revision IDs, revoke/404 and private-safe RSS results without actual private note bodies. The fresh trace run passes 11/11, the final exclusive Web and server suites pass 352/352 and 239/239 ordinary cases, both typechecks/builds pass, ordinary Chromium passes 75/75 and real Fastify passes 4/4. All 44 P4-T6/P4-T7 visual artifacts are byte-identical to the opened review set after the final browser run. Reverse audit derives KNOW-01 23/23 and OBS-01 4/4 `verified-local`; PUBLISH-01 32/32 and LIFE-20 4/4 remain `partial` only because their later immutable-container-image evidence is absent. P4 is locally closed without implementing P5 or claiming Web/UHub completion.  
**P5-T1 product evidence:** the first focused configuration run failed 11/11 required behaviors, while the planned safe-fetch and redaction modules failed to load before implementation. The minimum server-only layer now keeps six integrations disabled by default, requires enabled HTTP(S) origins, excludes credentials and internal base URLs from JSON status serialization, enforces 500–10,000 ms deadlines and 64 KiB–2 MiB response limits, compares exact URL origins, rejects raw query languages/redirects/userinfo/lookalike origins, counts bytes before JSON parsing and recursively removes secret/body/Kubernetes annotation data. Focused security passes 25/25, server typecheck exits 0 and the complete server regression passes 264 ordinary tests with only 48 separately owned exact-MySQL cases skipped. The 27 P5-T1 operations/security foundation atoms advance to `partial`; PLATFORM-01 and SEC-01 remain pending because adapters, routes, browser journeys, image and rendered RBAC evidence are later tasks.  

**P5-T2 product evidence:** the first adapter command retained all P5-T1 integration-security tests and failed on the six planned missing cache/adapter modules. Minimum implementation adds a visibility-independent 15-second server cache with coalescing and three-second failure containment; approved Kubernetes node/deployment/pod/Service/HTTPRoute reads; nine server-owned Prometheus queries; firing/recently-resolved Alertmanager normalization; a fixed 100-row Elasticsearch template with exact filter values and sanitized bounded events; and GitHub/Argo delivery normalization with immutable Web/API digest parsing and source-local failure. A separate service-account RED proved token/CA paths were initially ignored; the bounded HTTPS transport now reads both configured files, uses the CA for verification and never throws the token. Focused integration passes 40/40, server typecheck exits 0 and complete server regression passes 290 ordinary tests with 48 exact-only skips. All 42 required adapter/data/security atoms are partial; PLATFORM-01 and SEC-01 stay pending until routes/UI/E2E and later image/RBAC evidence.  
**P5-T3 product evidence:** exact `prom-client@15.1.3` supplies process defaults plus HTTP active/duration/count metrics whose labels are limited to method, normalized route and status class. Seven authenticated platform routes normalize the P5-T2 adapters, reject raw query languages and unknown log filters, keep stable cache timestamps and contain each source failure; public `/metrics` exposes Prometheus content type without a session cookie. The private `/app/platform` surface is one bright continuous canvas with eight-source connection truth, topology/alerts/trends/logs/delivery regions, all seven tabs, accessible SVG title/description/table/unit/pattern fallbacks, correct Jenkins/UHub/Harbor status, visibility-only 30-second polling and abort-on-change/unmount. A real 390×844 rejection found the current ten-item primary Platform destination outside the scroll viewport; route/resize-aware centering now passes 390 and 320 without document overflow or hiding the approved IA. Fresh gates pass focused server 8/8, focused Web 11/11, full server 298 ordinary tests plus 48 exact-only skips, full Web 79 files/359 tests, dual typecheck/build and focused Chromium 2/2. The primary executor opened the final 1440×900, 1024×768, 768×1024, 390×844 and reduced-motion images. PLATFORM-01 is atom-derived `partial`; SEC-01 stays pending on later rendered-RBAC least-child evidence. P5-T4 through P5-T7, MySQL data evidence and later delivery work remain open.  

**P5-T4 product evidence:** migration `015_search.sql` adds the owner-scoped search index, historical backfill and transaction-local MySQL triggers for goal/project/task/record/review/knowledge/public draft plus Life item/recipe/medicine/fitness/household/shopping/day-plan/cooking-record changes; soft-delete and rollback behavior remain atomic. The bounded route accepts only the fifteen approved types, two-or-more-character queries and limit at most 50, escapes literal `%`/`_`, excludes other owners/deleted facts and returns plain-text excerpts with deterministic title/tag/body/recency ranking and recipe/day-plan context. The command overlay uses a 180ms debounce, aborts stale work, groups results, preserves recent routes privately, supports keyboard selection/Escape/focus restoration and renders no HTML highlights. Fresh gates pass focused server 14/14, focused Web 12/12 including layout regression, full server 312 ordinary tests plus 49 exact-only skips, exact official MySQL Community Server 8.4.10 49/49 with zero skip, full Web 81 files/366 tests, dual typecheck/build, focused Chromium 1/1 and full Chromium 78/78. The primary executor opened 1440×900, 1024×768, 768×1024, 390×844, 320×900 and reduced-motion artifacts. All 44 search-surface atoms are `verified-local`; GLOBAL-01 remains pending on P5-T5/P5-T6 and LIFE-21 remains partial on P5-T5.  
**P5-T6 product evidence:** additive migration 016, owner-scoped Memory/MySQL orchestration and authenticated routes now provide versioned preferences, current-password/new-password enforcement, safe other-session revocation, immutable secret-free audit events, truthful configured/degraded/disabled integration status and deterministic schema-v1 export. Preview performs no writes and reports counts/conflicts/rejections/checksum; apply repeats normalization, duplicate/relation/collision/ledger checks inside one owner transaction, requires the exact preview checksum and current password, persists and verifies a full-account restore point first, preserves IDs for same-owner import and remaps the entire reference graph for cross-owner import. Deleted record/review/knowledge facts remain restorable with their trash tombstones. Fresh focused, complete Web/Server, official MySQL 8.4.10 50/50, dual type/build and complete Chromium 81/81 gates pass. Seven settings captures and the two regenerated private-route filmstrips were opened and accepted. The 21 settings-route atoms and four scoped transaction-safety atoms are `verified-local`; the two LIFE-19 transfer atoms remain `partial` until P6 image evidence, and GLOBAL-01 remains pending until P5-T7.  
**P5-T7 product evidence:** formal platform/global acceptance covers all seven platform tabs, URL-restored tab/metric/log filters and Browser Back, connected/degraded/disabled truth, visibility-paused polling, local retry, raw-query rejection, secret-free serialization, read-only Kubernetes boundaries and anonymous-route rejection. Search and Quick Create retain keyboard focus, context, idempotency and exact undo; all nine settings categories, password/session controls and preview-before-apply data transfer pass desktop and mobile journeys. Valid REDs corrected mobile status order and made deep-linked late tabs visible without moving the route-stage. Fresh gates pass Web 84 files/386 tests, server 334 ordinary tests plus 50 exact-only skips, official isolated MySQL 8.4.10 at 50/50 zero skip, dual typecheck/build, focused Chromium 17/17 and real Fastify Chromium 4/4. The primary executor opened and accepted all 52 final platform/global/settings screenshots and normal/reduced filmstrips at 1440/1024/768/390/320 and 200% zoom.  
**P5-T8 closure evidence:** `outputs/final/platform-global-verification.md` records the adapter fixtures, 15-second success/three-second failure cache, visibility-only polling, recursive response redaction, search/Quick Create/settings journeys, browser/OS/font/DPR/viewport/color/motion metadata, both lock hashes and all 84 P5 screenshot/filmstrip paths; it explicitly records that P5 retained no trace archive instead of inventing one. The reverse audit confirms PLATFORM-01 141/141, GLOBAL-01 72/72, LIFE-21 56/56 and LIFE-22 127/127 `verified-local`, while LIFE-19 remains 32/32 `partial` at the P6 image boundary. The first P5 phase-close correctly failed on P5-T8 plus eight pending SEC-01 children; a fresh login/records/settings/bounded-fetch/redaction command passed 5 files/32 tests and supplies their honest partial evidence. SEC-01 is therefore 18/18 `partial`, not complete, until P6 image/RBAC proof. P5 is locally closed without claiming Git, Docker, GitHub, UHub, digest, SBOM/provenance or cluster state.  
**P2-T6 evidence-status correction:** the approved correction changes only the stale work-package closure wording and its synchronized source clause. The 108 public/auth atoms still require exact-digest `image` evidence and retain `[local,image]` final boundaries. Local unit/API/E2E/visual/a11y/security evidence therefore derives `partial`; P6 exact-image evidence can later derive `verified-image`. The matrix, image definition and derivation logic are unchanged.  
**P1-T13 historical changed evidence paths:** `server/src/mysql.integration.test.ts` SHA-256 `ED154B72F60CF9890318AAA8EF510600A95C6F4C55A0F3A1091A90C4E9280057`; `server/src/app.test.ts` `C7CDB5BC826B4F0547645247870C7BD4E03FC3125DB5BC9044E5476D724A19EB`; `src/api/lifeApi.test.ts` `B4C98D5E399B64FDD5EF44C838F2353E474AF1575D84CD0D0813F003C1B7E4B3`. At that historical checkpoint the workspace was non-Git, so these were local SHA-256 evidence rather than commit IDs. The task-only official MySQL 8.4.10 instance was identity-verified on port 34024, cleanly shut down with exit 0 and then returned a failed ping as required; its workspace data directory is retained. No Kubernetes capability was requested or used.
**P2-T5 close boundary:** P2-T1 through P2-T5 prove the local public object model, exact shared geometry/motion lifecycle, one-scene composition, day/night/login golden slice, published-copy detail reads, five distinct detail journeys, exact return-state restoration, four-viewport and 320 CSS px reflow, collision-safe labels and reproducible screenshot/filmstrip/trace capture. They do not claim P2-T6 full-plan closure, atom-backed parent rollup, exact-image smoke or UHub delivery.  
**Accumulated historical narrative (non-authoritative; retained for audit):** the execution guard and P1-T1 through P1-T8 are complete; P1-T9 Step 1 was the next product work at that historical checkpoint. P1-T7 adds deterministic evidence reviews, narrative-preserving versioned drafts, archive/soft-delete/restore, transactional one-time action conversion, audit records and typed frontend API contracts without moving any parent requirement out of `pending`. The later rows and checkpoints preserve subsequent P1 progress. The literal current root is intentionally recorded in the excluded dynamic evidence manifest and handoff ledgers rather than this included source-root input, avoiding self-reference. The workspace remains non-Git; Docker Desktop/Buildx are now locally verified, while valid GitHub auth, UHub and formal release digests remain unverified. The installed system MySQL service remains out of scope while isolated exact 8.4.10 application gates are verified per task and shut down afterward.

| ID | Requirement | Planned tasks | Status | Fresh local evidence | Required final evidence |
|---|---|---|---|---|---|
| PUB-01 | Public orbit and five semantic objects | P2-T1, P2-T2, P6-T5, P6-T8 | verified-local | All local/public children are complete and the image-bound children pass the exact released Web/API image plus fresh Linux browser gate. ADR-029 geometry, masked rings, five semantics, unlit center, approved login depth, responsive inset, three-browser continuity and opened P6-T8 final images map through the final index. The least-child rollup remains `verified-local` because this parent intentionally contains both local- and image-final atoms. | complete; production DNS/TLS and cluster routing remain user-owned |
| PUB-02 | Five page-native public details and phase-exact return | P2-T4, P6-T8 | verified-image | Fresh official Linux Chromium/Firefox/WebKit coverage, real-Fastify public reads, the P6-T8 detail screenshots and return trace restore the complete five-detail/phase-exact-return contract. Exact released Web/API image evidence closes every image-final atom. | complete at immutable-image boundary; production hostname reachability is unobserved |
| AUTH-01 | Login-first golden slice, astrolabe shift and real auth | P2-T3, P6-T5, P6-T8 | verified-local | Unit/API/real-Fastify and three-browser gates prove form/auth/focus/Escape/reopen continuity, dark night/light day surfaces, exact `520/32/460` wide composition, compact/full-screen behavior, no replay/clipping and the approved orbit-left/title-recede sequence. Exact released images close image-final children; the mixed local/image parent rolls up to `verified-local`. | complete; no credential value is recorded |
| APP-01 | Bright private shell and overview | P3-T1, P6-T8 | verified-image | Stable daylight shell, exact top navigation, continuous overview, deterministic model, canonical redirects, scoped failures, keyboard/focus/Back/reduced-motion, current API/MySQL and reviewed multi-width imagery remain source-current. Exact released Web/API route smoke closes all 28 image-final atoms. | complete at immutable-image boundary |
| GOAL-01 | Goals/projects/milestones | P1-T3, P3-T2 | verified-local | Atom-derived local close: official MySQL 8.4.10 proves independent risk persistence, same-ID owner/version restore, archive-linked compensating audit and failed-operation rollback; Memory/Fastify/frontend API tests cover parity and CSRF. The 8/4 outcome map, complete editors, URL/history inspectors, mobile fixed task layers, keyboard/focus/reduced-motion and truthful loading/empty/network/offline/403/409 states pass current four-viewport plus 320 CSS px E2E, visual review, filmstrip and trace evidence | MySQL/API/UI/E2E/persistence and audited recovery |
| SCHEDULE-01 | Tasks/schedule/recurrence | P1-T4, P3-T3 | verified-local | Atom-derived local close: pure 15-minute geometry uses timezone-local dates, enforces a 15-minute minimum, rejects cross-midnight changes and preserves exact undo state; authenticated Fastify/Memory/MySQL gates cover owner isolation, versioning, idempotency and rollback. The continuous daylight day/week/month workspace, pools, warnings, complete task editor, one-mutation pointer preview, keyboard scheduling/undo, 409 recovery, URL history/focus restoration, reduced motion and 1440/1024/768/390/320 reflow pass current unit/API/MySQL/E2E/a11y/visual/manual evidence | unit/MySQL/drag+keyboard E2E |
| HABIT-01 | Habit rhythm | P1-T5, P3-T4 | verified-local | Atom-derived local close: owner-scoped Fastify/Memory/MySQL gates cover schedule rules, goal/project links, idempotent creates, optimistic correction, pause/archive and entry transactions without fabricated missed rows. The continuous daylight today strip and 28-day matrix expose all six states with text/shape semantics, deterministic statistics, boolean/numeric/duration/quantity writes, partial/intentional-skip reasons, complete editing, URL/history, keyboard/focus/reduced-motion, scoped 403/409 recovery and 1440/1024/768/390/320 responsive evidence; no badge/flame/coin or streak-failure pressure is present | rule/MySQL/UI/six-state E2E |
| RECORD-01 | Records and media | P1-T6, P3-T5, P6-T1, P6-T8 | verified-image | Authenticated Fastify/Memory/MySQL, private media authorization, cover identity, autosave/upload/recovery and filesystem/S3 topology evidence remain current. Exact released API/Web image inspection and smoke close all 30 image-final atoms without reclassifying a local image as registry evidence. | complete; multi-replica filesystem still requires RWX or S3 |
| REVIEW-01 | Evidence reviews | P1-T7, P3-T6 | verified-local | Atom-derived local close: deterministic persisted aggregation preserves goal/project/task/habit/record facts and prior commitments while narrative remains user-authored. Weekly/monthly/custom URL modes, the continuous 3/6/3 evidence/narrative/action hierarchy, shared autosave and 409 recovery, truthful error-versus-empty state, refresh/archive/delete/same-ID restore, all four one-time conversions, mobile ordered layers, reduced motion, 200% reflow and opened 1440/1024/768/390 evidence pass current unit/API/MySQL/E2E/a11y/security/visual/manual gates | aggregation/action conversion E2E |
| KNOW-01 | Knowledge and resurfacing | P4-T1, P4-T2, P4-T6 | verified-local | Atom-derived local close: direct/derived CRUD, collections, factual source links, cycle-safe relations, deterministic due resurfacing, optimistic versions, archive/delete/same-ID restore, CSRF/owner isolation, typed cancellable queries and exact MySQL 8.4.10 persistence preserve legacy source projection. The authenticated 2.5/3.5/6 workspace, safe Markdown/autosave/conflict UI, responsive task layers, keyboard/Back/focus/scroll restoration and opened four-viewport/reduced-motion evidence all pass current gates | API/Markdown/search/E2E |
| OBS-01 | Controlled Obsidian sync | P4-T3 | verified-local | Atom-derived local close: stable knowledge/review frontmatter, pure no-delete revision/conflict planning, permission-gated read-only-first folder scan, deterministic ZIP preview/confirmation, backup-before-write atomic batches, explicit partial-failure reporting and encoded Obsidian links pass current focused/full Web and standalone Chromium evidence. P4-T6 adds the real `/app/settings` route, explicit per-conflict choice gate and formal fallback/denial/read-only-first/backup-before-write/no-delete journey | preview/conflict/backup/fallback E2E |
| PUBLISH-01 | Publishing and immutable versions | P4-T4, P4-T5, P4-T6, P6-T8 | verified-image | Domain/API/MySQL/security, privacy-gated workbench, source-copy preview, immutable history/diff, schedule/revoke/404/RSS and responsive/a11y/motion evidence remain current. Exact released Web/API public reads close all 32 image-final atoms. | complete at immutable-image boundary; source copies remain immutable revisions |
| PLATFORM-01 | Truthful platform center | P5-T1, P5-T2, P5-T3, P5-T7 | verified-local | Atom-derived local close: disabled-default bounded integrations, allowlisted cached adapters, application metrics, authenticated normalized read-only routes and the bright seven-tab center pass unit/API/MySQL/security/E2E/a11y/manual evidence. Connected/degraded/disabled states, URL-restored filters, hidden-page polling pause, scoped retry, accessible trend tables, mobile status order and visible late-tab deep links are current; no raw query, secret, mutation path or fabricated value enters the UI | adapter/security/UI plus connected/degraded/disabled/unverified fixtures without fabricated data |
| GLOBAL-01 | Search/quick record/settings | P5-T4, P5-T5, P5-T6, P5-T7 | verified-local | Atom-derived local close: owner-scoped MySQL search, context-aware 15-type Quick Create with idempotency/undo, all 21 Settings route atoms, password/session safety and checksum/restore-point data transfer pass current unit/API/MySQL/security/keyboard/E2E/a11y/manual evidence. Desktop/mobile search, quick record and nine-category settings preserve focus, selected state and preview-before-write semantics | keyboard/context/export/import E2E |
| MOTION-01 | Continuous interruptible spatial transitions with bounded engine ownership | P1-T2, P2-T3, P2-T4, P2-T5, P3-T7, P6-T5, P6-T6 | verified-local | Current local evidence covers separated whole-group/ring/counter/upright/material arrival, header pause, hidden/reduced suspension, login no-replay and return playheads. ADR-030 keeps Web Animations as the exclusive owner of four continuous ring rotations plus five upright counters and GSAP as the owner of title, arrivals, details and aperture; the login scene now has one cancelable matrix-interpolation owner because official Linux WebKit froze CSS/WAAPI/GSAP transform transitions under this exact layered composition. The owner preserves the unchanged 680 ms cubic Bézier, stable endpoints and interruption semantics. Fresh Chromium four-breakpoint, Firefox critical, WebKit critical and both dedicated theme-performance gates pass with workers=1/retries=0, and the final filmstrips/traces plus nine opened images are rebound to current source/artifact hashes | engine-owner unit/DOM contract, unchanged WebKit cadence/theme probe, filmstrip/trace, reduced motion and manual review |
| SPACE-01 | Continuous soft-volume space with page-native hierarchy | P2-T5, P3-T7, P6-T5 | verified-local | Chromium/WebKit/Firefox numerically prove wide/compact/full-screen relationships and complete outer-ring safe inset at 1440/1024/768/390/320, with zero horizontal overflow. The less-crowded 390 composition and desktop depth sequence were opened and user-approved; unrelated SPACE-01 children still cap the parent | four-view breakpoint screenshots, golden-slice comparison and manual whole-page veto audit |
| STATE-01 | Loading/empty/error/conflict states | P1-T2, P2-T5, P3-T7, P5-T7, P6-T5 | verified-local | Atom-derived local close: prior state evidence remains current; ADR-029 focused unit plus Chromium/WebKit/Firefox prove default night, manual day, private daylight, enhanced/fallback, login reopen and no-white-frame continuity. Final state imagery was opened and user-approved, including the unlit center correction | failure fixtures, recovery, offline, visual-state continuity and manual direction approval |
| DATA-01 | Versioned domains and migration | P1-T1, P1-T13, P6-T1, P6-T8 | verified-local | The final frontend/server/API gate plus official MySQL 8.4.10 50/50 covers the remaining unit/API types. API startup performs no schema mutation; the dedicated runner applies all 16 ordered checksum-locked migrations under the database lock, and the clean rehearsal/restore path passes. | complete locally; built-in MySQL is single-instance and not HA |
| SEC-01 | Session/CSRF/sanitize/secret boundary | P1-T1, P4-T4, P5-T1, P6-T2, P6-T8 | verified-image | Authenticated/CSRF owner isolation, bounded/redacted read-only integrations, exact get/list-only conditional RBAC, projected token/CA, production Secret rejection, non-root/drop-ALL workloads and scoped NetworkPolicies pass current API/E2E/security gates. Exact released Web/API image inspection closes all 18 image-final children. | complete; no secret value, kubeconfig or private authentication material was read |
| DELIVERY-01 | Images/UHub/GitOps/deployment package | P6-T4, P6-T6, P6-T7, P6-T8 | verified-local | Ordinary CI `33285063683`, release `33286877080`, exact Web/API digests, digest-bound SPDX/SLSA, exact-digest smoke, GitOps `03d8123`, registry inspection, Helm/package gates and the capability-first deployment/operations handoff are complete. P6-T8 adds the 44-row final index and repository-backed close manifest. Registry-final children are `verified-registry`; the least-child parent correctly rolls up to `verified-local` because it also contains local-final documentation atoms. | Web-owned delivery complete after final validator sync; Argo/cluster/DNS/TLS remain user-owned and unobserved |
| LIFE-01 | Life workspace and global summary | P3-T8, P3-T13, P6-T8 | verified-image | Stable `/app/life`, exact subnav, inventory route, Overview continuation, Today timeline, planned/actual nutrition/cost, shortages, failure states and reviewed responsive imagery remain current. Fresh aggregate visual/manual evidence plus exact released Web/API route smoke closes all 36 image-final atoms. | complete at immutable-image boundary |
| LIFE-02 | Corner calendar and complete day plan | P1-T11, P3-T8, P3-T13 | verified-local | Atom-derived local close: version-bound template/projection/completion and frozen actual snapshots combine with semantic month/summary states, roving keys, focus trap/Esc/Back restoration, persistent mobile exit and plan-only copy. The P3-T13 RED/GREEN proves one pending submission, duplicate suppression, preserved target date and scoped offline recovery | calendar API, states, copy/template, mobile close/back |
| LIFE-03 | Ingredient and supplement catalog | P1-T8, P3-T9 | verified-local | Atom-derived local close: ADR-027's discriminated factual profile passes server domain/route 22/22, typed client 5/5 and official MySQL 8.4.10 45/45; the unified UI passes 14/14 plus Chromium CRUD/search/bulk/undo/inventory and four-breakpoint gates | CRUD/nutrition/price/inventory/bulk MySQL+UI |
| LIFE-04 | Medicine inventory/expiry/user schedule | P1-T8, P1-T11, P3-T9, P3-T11 | verified-local | Atom-derived local close: medicine facts, occurrences and audited inventory history remain green; P3-T9 renders user-authored medicine/package/schedule facts and explicit no-advice copy, while P3-T11 adds user-fact-only delay/skip/backfill, owner-scoped occurrence persistence and current security/E2E evidence. Final image safety remains tracked under LIFE-24 | factual API/UI, batch/expiry/schedule and safety scan |
| LIFE-05 | Flexible units and item conversions | P1-T8, P3-T9 | verified-local | Atom-derived local close: P1-T13 proves converted policy/ledger facts; P3-T9 adds unit CRUD, built-in protection, item/package conversion editing, missing-conversion refusal and visible incomplete forecast evidence across unit/API/MySQL/Chromium | fixed/item-specific conversion and cross-dimension refusal |
| LIFE-06 | Price/nutrition future recalculation | P1-T8, P1-T10, P1-T11, P3-T9 | verified-local | Atom-derived local close: effective-price/incomplete foundations and frozen history remain green; P3-T9 adds dated price/nutrition facts and missing-conversion truth, while P3-T10 presents pinned version calculations, impact preview and incomplete-fact refusal | future update, historical freeze and impact preview |
| LIFE-07 | Recipe versions/cooking/growth | P1-T10, P3-T10 | verified-local | Atom-derived local close: P1 freezes selected versions, calculations and cooking snapshots through Memory/MySQL/Fastify; P3-T10 adds scaling, pinned history, explicit impact preview, persisted cooking progress, actual quantities, session-note promotion and factual completion across current unit/API/MySQL/E2E evidence | calculation/version/cooking/UI/E2E |
| LIFE-08 | Ingredient-recipe bidirectional relation | P1-T10, P3-T10 | verified-local | Atom-derived local close: the owner-scoped API query now reaches a route-addressed recipe↔ingredient workspace with interactive graph nodes, an always-available semantic list, reverse navigation and current-stock now/missing-one/expiring feasibility filters | query, trace links, graph/list and inventory feasibility |
| LIFE-09 | Week plans/templates/date copy | P1-T11, P3-T11 | verified-local | Atom-derived local close: version-bound conflict keep/merge/replace/skip, explicit apply/sync range, plan-only copy, day independence and completed-history preservation pass current domain/API/MySQL/UI/E2E evidence | conflict apply/sync/copy API+E2E |
| LIFE-10 | Supplement/medicine/fitness scheduling | P1-T11, P3-T11 | verified-local | Atom-derived local close: supplement meal-time linkage, persisted medicine occurrence delay/skip/backfill, fitness composition, user-estimated energy, actual-duration completion, duplicate-submit protection and state-aware undo pass current Memory/MySQL/Fastify/UI/E2E evidence | recurrence/complete/skip/delay/backfill E2E |
| LIFE-11 | Inventory batches/ledger/forecast/idempotency | P1-T9, P1-T11, P1-T12, P3-T11 | verified-local | Atom-derived local close: the P1-T13 full journey proves 1,000g opening stock → 400g cooking consume → 100g partial purchase → 500g plan consume → 50g refund leaves 150g across reconnect. P3-T11 adds one durable completion/reversal UI and P3-T12 adds purchase/refund UI while preserving immutable history and atomic stock/spend/list closure | MySQL exact-once, reversal, retry and forecast |
| LIFE-12 | Prepared-food stock | P1-T10, P3-T10 | verified-local | Atom-derived local close: exact MySQL preserves make-four/eat-one and the three-portion remainder without double ingredient deduction; P3-T10 previews consumption and remainder before submission, completes once after server confirmation and refreshes prepared-food totals | make-many/eat-one remainder and no double ingredient deduction |
| LIFE-13 | Household consumable/durable hierarchy | P1-T8, P3-T9 | verified-local | P3-T9 completes the approved consumable purchase/cycle/depletion and durable value/lifecycle/warranty/maintenance/retirement/set profiles through domain, Fastify, Memory-compatible cloning, existing MySQL JSON and client/UI; unit plus mobile inspector E2E prove value/state labels and reversible return | CRUD/tree drag+keyboard/value/state E2E |
| LIFE-14 | Unified shopping/purchase/refund | P1-T12, P3-T12 | verified-local | Atom-derived local close: server and exact MySQL preserve manual/formal distinctions, partial purchase, rounded remainder recalculation, atomic inventory/spend/list closure and compensating refund; P3-T12 adds merged reasons, store/priority grouping, actual-price confirmation and current UI/E2E evidence | suggestion dedupe, partial purchase, atomic stock/spend, refund |
| LIFE-15 | Cash expenditure/consumption cost/budgets | P1-T12, P3-T8, P3-T12 | verified-local | Atom-derived local close: exact MySQL and UI keep cash expenditure separate from immutable consumption cost, label planned/actual/incomplete facts, expose budget warning/critical thresholds and retain source drill-down across current API/unit/E2E evidence | separate calculations, thresholds, forecast and drill-down |
| LIFE-16 | Traceable life analytics | P1-T12, P3-T12 | verified-local | Atom-derived local close: no-record, zero, planned, actual and incomplete remain distinct; every SVG point has a semantic table equivalent and source-fact drill-down with Browser Back restoration across current unit/API/MySQL/E2E evidence | ranges, no-record, accessible chart/table and source links |
| LIFE-17 | Taxonomy/tags/order/bulk operations | P1-T8, P3-T9 | verified-local | Atom-derived local close: P3-T9 adds dense category/tag/location CRUD, hierarchy/cycle guard, keyboard reorder, batch preview and versioned undo. Focused UI 14/14, API/MySQL evidence and the opened Chromium dense-taxonomy/batch filmstrip are green | custom hierarchy, cycle guard, impact preview, undo |
| LIFE-18 | Trash/restore/reference protection | P1-T8, P3-T9 | verified-local | Atom-derived local close: P3-T9 adds relationship impact preview, soft delete, trash listing and same-identity restore; permanent delete remains unavailable unless the API proves reference safety. Unit/API/MySQL and browser evidence are green | soft delete, warning, restore and permanent-delete gate |
| LIFE-19 | Versioned backup/import/export | P1-T12, P3-T12, P5-T6, P6-T8 | verified-image | Schema/checksum export, no-write preview, graph/ledger validation, password confirmation, restore point and atomic apply/audit rollback pass current MySQL/UI/E2E evidence. Exact released Web/API image smoke closes all 32 image-final atoms. | complete; import remains preview-first and all-or-nothing |
| LIFE-20 | Controlled life Obsidian projection | P4-T7, P4-T8, P6-T8 | verified-image | Deterministic allowlisted Markdown/ZIP, preview-only conflict/version planning, backup-before-write, permission/unsupported/offline states and responsive/Back/focus evidence remain current. Exact released Web/API controlled-projection smoke closes all four image-final atoms. | complete; direct writes remain permission-gated and ZIP remains preview-only |
| LIFE-21 | Life search/trace/quick create | P3-T13, P5-T4, P5-T5 | verified-local | Atom-derived local close: P3-T13 proves Life route/overlay keyboard, focus, trace and return behavior; P5-T4 verifies owner-scoped Life search, relation context, filters, keyboard and responsive command navigation; P5-T5 adds loaded-ID-only context inheritance, all supported Life create adapters, server-confirmed retry/idempotency/undo, focus restoration and opened responsive/reduced-motion evidence | stable routes, relation context, keyboard and return restoration |
| LIFE-22 | Network/version/offline/duplicate safety | P1-T9, P1-T10, P1-T11, P1-T12, P3-T13, P5-T6 | verified-local | Atom-derived local close: ledger replay/reversal, optimistic conflicts, occurrence/restore/recalculation concurrency, owner-consistent snapshots, Memory FIFO/read isolation, commerce idempotency, failed-import rollback and complete delayed/403/409/500/offline/duplicate-submit UI journeys are current. Risky offline calendar copy remains pending instead of reporting success | 409, pending draft, retry/idempotency and failed-import rollback |
| LIFE-23 | Life responsive/a11y/motion | P3-T8, P3-T9, P3-T10, P3-T11, P3-T12, P3-T13, P6-T5, P6-T8 | verified-image | Today/calendar, catalog, recipes, planning/fitness, shopping/analytics/data and comprehensive routes pass current five-width, 200% zoom, reduced-motion, keyboard/focus/Back, scoped-failure and zero-overflow evidence. Exact released Web image route/motion smoke closes all 133 image-final atoms. | complete at immutable-image boundary |
| LIFE-24 | Medicine safety and life privacy | P1-T8, P3-T9, P3-T13, P6-T2, P6-T8 | verified-image | Owner isolation, forbidden-advice rejection, profile allowlists, user-authored medicine copy, security/failure journeys, production Secret rejection, read-only RBAC, projected-token containment, scoped NetworkPolicies and hardened workloads remain current. Exact released Web/API inspection closes all 45 image-final children. | complete; no diagnosis, dosage or unverified interaction advice |

## Ledger rules

- These 44 rows are parent rollups, not proof that every child behavior is complete. Once the ADR-018 atomic matrix exists, the parent status is derived from its least-complete applicable child atom.
- The atomic guard, `startup`/`handoff` and user-triggered fresh-task drill now pass, but every parent remains `pending` until its active product task supplies current evidence; a manual status edit cannot bypass atom derivation.
- A row cannot leave `pending` until its active task writes a failing behavioral test and execution-control records the red command/exit code.
- `verified-local` requires current source, automated tests and required browser/visual evidence; a skipped MySQL, remote E2E or visual review is not a pass.
- `verified-image` requires smoke evidence against the intended immutable Web/API digests; `verified-registry` additionally requires UHub inspect, SBOM/provenance, matching production values and the validated deployment package. Only rows whose final boundary needs those artifacts may use the higher status.
- Argo `Synced/Healthy`, Kubernetes context and cluster-entry smoke are optional user deployment records, not states or completion gates in this Web ledger.
- Evidence paths must name sanitized artifacts. Credentials, raw personal content and private vault bodies never enter this file.
- Any plan/spec change updates this ledger before implementation resumes.

## 2026-08-30 CI19 current evidence override

Ordinary CI `33267670149` / job `99140524702` at `13b7a54` passed unit/type/build, official MySQL and browser installation, then failed only dedicated WebKit theme performance at transition P95/max `41/76ms` against unchanged `34/100ms` budgets. Official-image diagnostics isolated the independent 420ms `.theme-switch__mark` transition; focused TDD first failed 1/4, and the minimum correction removes only that decorative transition while preserving its endpoint and every ring owner/period.

Fresh gates pass focused 4/4, frontend 88/88 files and 425/425 tests, both typechecks/builds, server 362 ordinary plus 50 exact-only skips, official MySQL 50/50, WebKit theme repeated 20/20, Firefox theme, real-Fastify 12/12, Helm/media/security/observability/workflow/release contracts, audits, current-source Web/API image smoke, 16-migration dump/restore and Lighthouse 1.00/1.00/0.96/0.91. The full official matrix executed 335/336 with the sole failure being an exact LF check after Windows `git archive` populated the isolated volume with CRLF; restoring source LF bytes and rerunning the entire affected `public-final` file passed 5/5. No behavioral failure, test, browser, worker, retry, threshold or coverage was hidden.

Eight CI19 1440×900/390×844 day/night rest/login images were opened individually; metrics record 71 frames at 16.8ms P95/max and eight viewport/theme diagnostics with zero overflow and five labels. The deterministic checkpoint is generated after this current ledger text is frozen and binds 612 inputs, 462 same-order evidence rows and 83 visual states. Parent truth remains 30 `verified-local` / 10 `partial` / 4 `pending`; registry-bound atoms remain open until a new ordinary CI and the authorized immutable release actually succeed.

## 2026-08-30 CI20 current evidence override

Ordinary CI `33271354230` / job `99150269743` at `386de70ef9762074445b962c3c594f2e52dac2d2` passed all non-browser gates and 335/336 browser cases, with the sole failure being WebKit private-route continuity retaining nine healthy frames against the unchanged minimum of ten. Focused TDD first timed out under a deterministic foreground-WebKit no-rAF fixture, then passed after each probe sample raced rAF against a 16ms fallback while preserving the 360ms duration and ten-frame threshold. A separate repeated 768 night-home Axe reproduction failed 3/20 because the final orbit label was still fading; waiting for all five existing labels to reach opacity 1 made the same run pass 20/20 without changing WCAG rules, colors, geometry or motion.

Fresh gates pass frontend 88/88 files and 426/426 tests, frontend typecheck/build, server 362 ordinary tests plus 50 exact-only skips and server typecheck/build, disposable official MySQL 8.4.10 exact 50/50, all declared Helm/media/security/observability/workflow/release/data contracts, current-source local Web/API builds and `image-smoke: ok`, and official Linux Playwright WebKit theme 1/1 + Firefox theme 1/1 + matrix 336/336 = 338/338 with `workers=1` and `retries=0`. The CI19 final visual images remain product-source-current and were already opened individually; no product or visual source changed in CI20. Parent truth remains 30 `verified-local` / 10 `partial` / 4 `pending`; P6-T6 Step 7 and all registry-bound atoms remain open pending a new green ordinary CI and the single authorized immutable release.

## 2026-08-30 release #2 pipeline-remediation evidence override

Ordinary CI `33278665288` / job `99169909250` at `01f1bbb1450a92b719d9a3ddb84fd945c63eeb85` passed every gate in 35m03s. The single authorized `1.0.0` release run `33280128021` / job `99173780692` then independently passed full application checks, official MySQL and 36m13s Playwright/accessibility. It failed before registry sign-in because the direct Linux `pwsh` Helm pipeline supplied PowerShell pipeline records rather than Console stdin, and the observability validator therefore rejected an empty render. No UHub login, image build/push, digest, attestation, exact-digest smoke or GitOps digest update executed.

Focused TDD reproduced the direct-pipeline empty-render failure and now passes after the validator accepts `ValueFromPipeline` records while retaining the existing external-process stdin fallback. Windows PowerShell 5.1, PowerShell 7, the exact release-style Helm pipeline, workflow, release-production and release-preflight contracts are green with every ServiceMonitor, internal metrics Service, PrometheusRule, dashboard, runbook and non-public `/metrics` assertion unchanged. Parent truth remains 30 `verified-local` / 10 `partial` / 4 `pending`; DELIVERY-01 and P6-T6 Step 7 stay open. A fresh ordinary CI and new explicit release authorization are required before another dispatch.

## 2026-08-30 CI21 remote production-preview remediation evidence override

Ordinary CI `33282198354` / job `99179121484` at `39e5b5dff880e75bb4046e07d217cb451ccf74a5` passed frontend unit/type/build, official MySQL 8.4.10 and the complete 338-case browser/accessibility matrix. The remote real-Fastify suite then passed 11/12 and failed only the final WebKit login/write journey: navigation reached `/app/overview`, but the schedule link did not become actionable within the unchanged 15-second action boundary while the old Vite development server compiled lazy private chunks on demand.

The focused harness contract first passed 14 assertions and failed exactly one until `test:e2e:remote` built the Web production bundle and `tests-remote/globalSetup.ts` served `dist` through Vite preview on the same strict loopback port. Current local evidence passes that contract 15/15, the exact WebKit journey 1/1, a ten-repeat WebKit run 10/10, complete Chromium plus WebKit remote coverage 8/8, frontend 88/88 files and 427/427 tests, typecheck, the 885-module production build, image-smoke, observability direct/external pipeline, exact release-style Helm, workflow/release and Helm render/media contracts. Workers, retries, action timeout, browser coverage and product source are unchanged.

Local Windows Firefox failed before any page or LifeOps assertion ran: the Playwright browser log records tab-subprocess `SpawnTarget Error:0`, three GPU-process failures and `D3D11_NO_DEVICE` during context creation. This is retained as an environment blocker, not called green; the next ordinary Linux CI must verify current-source Firefox and full remote 12/12. Product/visual source is unchanged, parent truth remains 30 `verified-local` / 10 `partial` / 4 `pending`, and no UHub digest, attestation, exact-digest smoke or release success is claimed. The consumed release authorization is not renewed by this remediation.
