# 私人宇宙连续动效实施计划

> **状态：已否决并被取代，仅保留历史审计，禁止执行。** ADR-016 已明确取消私人区星球、轨道和宇宙布局；当前唯一实施入口是 `2026-08-09-00-lifeops-final-master-plan.md` 及其 `01` 至 `06` 工作包。不得从本文件恢复任何私人宇宙设计。

1. 先写失败测试：私有路由嵌套壳层、导航启用 view transition、星球有持续轨道时钟、减少动态退化。
2. 把 `/app/*` 重构为持久 `PrivateUniverseLayout` + `Outlet`，总览与功能共享背景、星轨和坐标导航。
3. 增加登录 portal 状态与共享元素命名；修正退出反向过渡。
4. 设计私有主题令牌、偏心轨道和功能近景；移动端改为可触摸的环形/底部坐标。
5. 用 Playwright 测连续性、帧稳定、焦点、响应式与 reduced motion。
