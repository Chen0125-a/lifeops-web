# API 与 MySQL 实施计划

> **状态：已被最终实施计划取代，仅保留历史审计，禁止单独执行。** 当前唯一实施入口是 `2026-08-09-00-lifeops-final-master-plan.md`，数据/API 工作以 `2026-08-09-01-lifeops-foundation-data-plan.md` 为准。

1. 先写失败测试：密码摘要、会话生命周期、用户隔离、公开快照只读副本与撤回。
2. 建立 server TypeScript 项目、配置校验、错误合同和可注入 store。
3. 实现 MySQL schema/migrations、事务型 repository、引导管理员和会话清理。
4. 实现 auth、plans、records、reviews、knowledge、snapshots API 与 CSRF。
5. 将前端改为 async LifeClient；保留显式 demo adapter 供测试，不在生产默认启用。
6. 增加 API 集成测试、跨浏览器公开快照 E2E 和失败/加载/空状态。
